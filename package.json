{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "varion-app",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/varion-app-backend",
    "src/varion-app-frontend"
  ],
  "dependencies": {
    "@dfinity/auth-client": "^1.3.0",
    "@dfinity/ledger-icp": "^2.3.1",
    "@psychedelic/plug-connect": "^0.2.0",
    "@Psychedelic/plug-connect": "npm:@psychedelic/plug-connect@^0.2.0",
    "@types/react-router-dom": "^5.3.3",
    "eml-parser": "^2.0.3",
    "react-dropzone": "^14.2.3",
    "react-router-dom": "^6.23.1",
    "redact-pii": "^3.4.0",
    "reflect-metadata": "^0.2.2",
    "styled-components": "^6.1.11"
  },
  "devDependencies": {
    "@originjs/vite-plugin-global-style": "^1.0.3",
    "web-vitals": "^4.1.1"
  }
}
